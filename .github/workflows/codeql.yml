name: "CodeQL Security Analysis"

on:
  workflow_run:
    workflows: ["CI Build"]  # Запускается после успешного CI билда
    types:
      - completed
    branches: [ master, main, develop ]
  schedule:
    - cron: '0 0 * * *'  # Каждый день в полночь (00:00 UTC)
  workflow_dispatch:  # Ручной запуск

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    # Запускается только если CI билд прошел успешно (или при schedule/manual)
    if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"

